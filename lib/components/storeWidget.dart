import 'package:flutter/material.dart';

class itemPicPrice extends StatelessWidget {
  const itemPicPrice({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
